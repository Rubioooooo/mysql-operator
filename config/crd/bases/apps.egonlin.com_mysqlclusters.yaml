---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: mysqlclusters.apps.egonlin.com
spec:
  group: apps.egonlin.com
  names:
    kind: MysqlCluster
    listKind: MysqlClusterList
    plural: mysqlclusters
    singular: mysqlcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.primary
      name: Primary
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MysqlClusterSpec defines the desired state of MysqlCluster.
            properties:
              credentialsSecretName:
                description: |-
                  CredentialsSecretName is the name of an external immutable Secret in the
                  MysqlCluster namespace containing root-password and replication-password.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              image:
                description: Image is the MySQL container image.
                minLength: 1
                type: string
              masterService:
                description: |-
                  MasterService is the stable Service name used to route traffic to the primary.
                  It is retained for API compatibility and is immutable after creation.
                maxLength: 60
                minLength: 1
                pattern: ^[a-z]([-a-z0-9]*[a-z0-9])?$
                type: string
              replicas:
                default: 3
                description: Replicas is the desired number of MySQL members.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: ResourceRequirements defines resource requests and limits
                  for MySQL.
                properties:
                  limits:
                    description: ResourceLimits defines CPU and memory limits for
                      a MySQL container.
                    properties:
                      cpu:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: cpu limit must not be negative
                          rule: 'type(self) == int ? self >= 0 : sign(quantity(self))
                            >= 0'
                      memory:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: memory limit must not be negative
                          rule: 'type(self) == int ? self >= 0 : sign(quantity(self))
                            >= 0'
                    required:
                    - cpu
                    - memory
                    type: object
                  requests:
                    description: ResourceRequests defines CPU and memory requests
                      for a MySQL container.
                    properties:
                      cpu:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: cpu request must not be negative
                          rule: 'type(self) == int ? self >= 0 : sign(quantity(self))
                            >= 0'
                      memory:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                        x-kubernetes-validations:
                        - message: memory request must not be negative
                          rule: 'type(self) == int ? self >= 0 : sign(quantity(self))
                            >= 0'
                    required:
                    - cpu
                    - memory
                    type: object
                required:
                - limits
                - requests
                type: object
              slaveService:
                description: |-
                  SlaveService is the stable Service name used to route traffic to replicas.
                  It is retained for API compatibility and is immutable after creation.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z]([-a-z0-9]*[a-z0-9])?$
                type: string
              storage:
                description: StorageConfig defines the persistent storage requested
                  for each MySQL member.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size is the persistent storage capacity requested
                      for each MySQL member.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                    x-kubernetes-validations:
                    - message: storage size must be greater than zero
                      rule: 'type(self) == int ? self > 0 : sign(quantity(self)) ==
                        1'
                  storageClassName:
                    description: StorageClassName is the StorageClass used by MySQL
                      persistent storage.
                    minLength: 1
                    type: string
                required:
                - size
                - storageClassName
                type: object
            required:
            - credentialsSecretName
            - image
            - masterService
            - resources
            - slaveService
            - storage
            type: object
            x-kubernetes-validations:
            - message: masterService and slaveService must be different
              rule: self.masterService != self.slaveService
            - message: masterService is immutable
              rule: self.masterService == oldSelf.masterService
            - message: slaveService is immutable
              rule: self.slaveService == oldSelf.slaveService
            - message: credentialsSecretName is immutable
              rule: self.credentialsSecretName == oldSelf.credentialsSecretName
          status:
            description: MysqlClusterStatus defines the observed state of MysqlCluster.
            properties:
              conditions:
                description: Conditions describe the current reconciliation and availability
                  state.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              credentialsSecretUID:
                description: |-
                  CredentialsSecretUID is the UID of the immutable external credential
                  Secret accepted for this MysqlCluster lifecycle.
                type: string
              currentReplicas:
                description: CurrentReplicas is the number of MySQL members currently
                  observed.
                format: int32
                type: integer
              ha:
                description: HA records durable primary-failure observations and failover
                  progress.
                properties:
                  failover:
                    description: Failover records an active durable failover plan.
                    properties:
                      candidate:
                        description: Candidate is the canonical StatefulSet Pod selected
                          by GTID-safe election.
                        minLength: 1
                        type: string
                      candidateUID:
                        description: CandidateUID binds Candidate to the exact Pod
                          incarnation that was elected.
                        minLength: 1
                        type: string
                      failedPrimary:
                        minLength: 1
                        type: string
                      failedPrimaryGTIDSet:
                        description: |-
                          FailedPrimaryGTIDSet is the authoritative fenced-primary GTID set used by
                          election. Nil means no snapshot was captured; a pointer to "" is a valid
                          authoritative empty GTID set.
                        type: string
                      failedPrimaryServerUUID:
                        description: |-
                          FailedPrimaryServerUUID is the freshly observed MySQL server UUID used to
                          validate each candidate's replication-source identity.
                        minLength: 1
                        type: string
                      failedPrimaryUID:
                        minLength: 1
                        type: string
                      fenceMethod:
                        description: MysqlClusterFenceMethod identifies the authority
                          used to fence writes.
                        enum:
                        - MySQLSuperReadOnly
                        type: string
                      fenceState:
                        description: MysqlClusterFenceState describes the durable
                          state of failed-primary fencing.
                        enum:
                        - Pending
                        - Verified
                        - Blocked
                        type: string
                      fencedPrimaryUID:
                        type: string
                      stage:
                        description: MysqlClusterFailoverStage describes the durable
                          stage of an active failover.
                        enum:
                        - Fencing
                        - CandidateSelected
                        - Promoting
                        - Reconfiguring
                        type: string
                    required:
                    - failedPrimary
                    - failedPrimaryUID
                    - fenceState
                    - stage
                    type: object
                    x-kubernetes-validations:
                    - message: a verified fence must identify the failed primary UID
                      rule: self.fenceState != 'Verified' || (has(self.fencedPrimaryUID)
                        && self.fencedPrimaryUID.size() > 0 && self.fencedPrimaryUID
                        == self.failedPrimaryUID)
                    - message: CandidateSelected and later stages require a verified
                        fence and complete election proof
                      rule: self.stage == 'Fencing' || (self.fenceState == 'Verified'
                        && has(self.fencedPrimaryUID) && self.fencedPrimaryUID ==
                        self.failedPrimaryUID && has(self.candidate) && self.candidate.size()
                        > 0 && has(self.candidateUID) && self.candidateUID.size()
                        > 0 && has(self.failedPrimaryServerUUID) && self.failedPrimaryServerUUID.size()
                        > 0 && has(self.failedPrimaryGTIDSet))
                    - message: the election candidate must differ from the failed
                        primary identity
                      rule: self.stage == 'Fencing' || (self.candidate != self.failedPrimary
                        && self.candidateUID != self.failedPrimaryUID)
                    - message: Fencing must not carry candidate-selection proof
                      rule: self.stage != 'Fencing' || (!has(self.candidate) && !has(self.candidateUID)
                        && !has(self.failedPrimaryServerUUID) && !has(self.failedPrimaryGTIDSet))
                  failureCount:
                    description: FailureCount is the number of consecutive failure
                      observations for PrimaryUID.
                    format: int32
                    type: integer
                  firstFailureTime:
                    description: FirstFailureTime is when the current same-UID failure
                      sequence began.
                    format: date-time
                    type: string
                  primary:
                    description: Primary is the observed primary Pod name associated
                      with this HA decision.
                    type: string
                  primaryUID:
                    description: PrimaryUID is the observed primary Pod UID associated
                      with this HA decision.
                    type: string
                  state:
                    description: State is the current durable HA state.
                    enum:
                    - Healthy
                    - Suspected
                    - FailoverRequired
                    - FailoverInProgress
                    - Verifying
                    - Degraded
                    type: string
                required:
                - failureCount
                - primary
                - primaryUID
                - state
                type: object
              lastConvergedImage:
                description: LastConvergedImage is the image last proven common to
                  the controlled workload.
                type: string
              lastConvergedReplicas:
                description: |-
                  LastConvergedReplicas is the replica count of the most recently completed
                  stable lifecycle state.
                format: int32
                type: integer
              master:
                description: |-
                  Master is retained for compatibility with the original API.
                  Deprecated: use Primary.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level lifecycle state of the cluster.
                enum:
                - Pending
                - Initializing
                - Running
                - Degraded
                - Failed
                type: string
              primary:
                description: Primary is the currently observed primary MySQL member.
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of MySQL members currently
                  ready.
                format: int32
                type: integer
              replicaTransition:
                description: |-
                  ReplicaTransition records an active replica-count transition. Nil means no
                  replica-count transition is currently recorded.
                properties:
                  fromReplicas:
                    description: FromReplicas is the last stable replica count from
                      which the transition began.
                    format: int32
                    type: integer
                  targetReplicas:
                    description: TargetReplicas is the current desired replica count
                      for the transition.
                    format: int32
                    type: integer
                required:
                - fromReplicas
                - targetReplicas
                type: object
              slaves:
                description: |-
                  Slaves is retained for compatibility with the original API.
                  Deprecated: replica membership will be represented by the new status model.
                items:
                  type: string
                type: array
              upgrade:
                description: Upgrade records an active durable image upgrade; TemplateReady
                  is not completion.
                properties:
                  fromImage:
                    minLength: 1
                    type: string
                  stage:
                    description: MysqlClusterUpgradeStage describes the durable upgrade
                      control barriers.
                    enum:
                    - Preparing
                    - TemplatePending
                    - TemplateReady
                    type: string
                  targetImage:
                    minLength: 1
                    type: string
                required:
                - fromImage
                - stage
                - targetImage
                type: object
            type: object
            x-kubernetes-validations:
            - message: credentialsSecretUID is write-once and cannot be changed or
                cleared
              rule: '!has(oldSelf.credentialsSecretUID) || (has(self.credentialsSecretUID)
                && self.credentialsSecretUID == oldSelf.credentialsSecretUID)'
        type: object
    served: true
    storage: true
    subresources:
      status: {}
